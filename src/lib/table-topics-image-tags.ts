// Add one entry here for every image you upload to
// public/table-topics-images/. Tags should be simple, lowercase words
// describing what's in the picture (e.g. "beach", "office", "family",
// "dog"). Multiple words and near-matches (like "beach" vs "beaches")
// are handled automatically, so keep tags short and simple.
//
// Tagging is optional: an image with no entry here (or with no tag that
// matches what someone typed) is still included in the random pool —
// it's just not prioritized when a theme is typed. So you can upload
// images and tag them gradually.
export const IMAGE_TAGS: Record<string, string[]> = {
  // "beach-sunset.jpg": ["beach", "sunset", "vacation", "ocean", "relax"],
  // "office-meeting.jpg": ["work", "office", "meeting", "business", "team"],
};